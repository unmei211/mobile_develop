package com.example.dotaapp.firstpage.data

import com.example.dotaapp.R
import com.example.dotaapp.firstpage.models.Person

val reviewerFirst = Person( nameId  = R.string.first_review_name,
    dataId  = R.string.first_review_data,
    imageId = R.drawable.first_head,
    descId =  R.string.first_review_desc,)

val reviewerSecond = Person( nameId  = R.string.second_review_name,
    dataId  = R.string.second_review_data,
    imageId = R.drawable.second_head,
    descId =  R.string.second_review_desc,)